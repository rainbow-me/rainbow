diff --git a/node_modules/@polymarket/clob-client-v2/dist/client.js b/node_modules/@polymarket/clob-client-v2/dist/client.js
index 7b1cbda..ad3fcc8 100644
--- a/node_modules/@polymarket/clob-client-v2/dist/client.js
+++ b/node_modules/@polymarket/clob-client-v2/dist/client.js
@@ -25,7 +25,6 @@ export class ClobClient {
     // Used to perform Level 2 authentication
     creds;
     orderBuilder;
-    tickSizes;
     negRisk;
     feeInfos;
     // Fee rate bps data for CLOB V1
@@ -47,7 +46,6 @@ export class ClobClient {
             this.creds = creds;
         }
         this.orderBuilder = new OrderBuilder(signer, chain, signatureType, funderAddress, getSigner);
-        this.tickSizes = {};
         this.negRisk = {};
         this.feeRates = {};
         this.feeInfos = {};
@@ -114,7 +112,6 @@ export class ClobClient {
                 continue;
             const tokenId = token.t;
             this.tokenConditionMap[tokenId] = conditionID;
-            this.tickSizes[tokenId] = result.mts.toString();
             this.negRisk[tokenId] = result.nr ?? false;
             this.feeInfos[tokenId] = {
                 rate: result.fd?.r ?? 0,
@@ -134,18 +131,14 @@ export class ClobClient {
         });
     }
     async getTickSize(tokenID) {
-        if (tokenID in this.tickSizes) {
-            return this.tickSizes[tokenID];
-        }
         if (tokenID in this.tokenConditionMap) {
-            await this.getClobMarketInfo(this.tokenConditionMap[tokenID]);
-            return this.tickSizes[tokenID];
+            const result = await this.getClobMarketInfo(this.tokenConditionMap[tokenID]);
+            return result.mts.toString();
         }
         const result = await this.get(`${this.host}${GET_TICK_SIZE}`, {
             params: { token_id: tokenID },
         });
-        this.tickSizes[tokenID] = result.minimum_tick_size.toString();
-        return this.tickSizes[tokenID];
+        return result.minimum_tick_size.toString();
     }
     async getNegRisk(tokenID) {
         if (tokenID in this.negRisk) {
diff --git a/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.js b/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.js
index baaea82..e7bb1e1 100644
--- a/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.js
+++ b/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.js
@@ -1,24 +1,10 @@
 /* eslint-disable max-depth */
 import axios from "axios";
-import { isBrowser } from "browser-or-node";
 export const GET = "GET";
 export const POST = "POST";
 export const DELETE = "DELETE";
 export const PUT = "PUT";
-const overloadHeaders = (method, headers = {}) => {
-    if (isBrowser) {
-        return;
-    }
-    headers["User-Agent"] = `@polymarket/clob-client`;
-    headers.Accept = "*/*";
-    headers.Connection = "keep-alive";
-    headers["Content-Type"] = "application/json";
-    if (method === GET) {
-        headers["Accept-Encoding"] = "gzip";
-    }
-};
 export const request = async (endpoint, method, headers, data, params) => {
-    overloadHeaders(method, headers);
     return await axios({ method, url: endpoint, headers, data, params });
 };
 const sleep = (ms) => new Promise(res => setTimeout(res, ms));
diff --git a/node_modules/@polymarket/clob-client-v2/dist/signing/hmac.js b/node_modules/@polymarket/clob-client-v2/dist/signing/hmac.js
index f624de9..e0619e6 100644
--- a/node_modules/@polymarket/clob-client-v2/dist/signing/hmac.js
+++ b/node_modules/@polymarket/clob-client-v2/dist/signing/hmac.js
@@ -1,4 +1,5 @@
-import crypto from "node:crypto";
+import { hmac } from "@noble/hashes/hmac";
+import { sha256 } from "@noble/hashes/sha2";
 function replaceAll(s, search, replace) {
     return s.split(search).join(replace);
 }
@@ -16,8 +17,8 @@ export const buildPolyHmacSignature = (secret, timestamp, method, requestPath, b
         message += body;
     }
     const base64Secret = Buffer.from(secret, "base64");
-    const hmac = crypto.createHmac("sha256", base64Secret);
-    const sig = hmac.update(message).digest("base64");
+    const digest = hmac(sha256, base64Secret, Buffer.from(message, "utf8"));
+    const sig = Buffer.from(digest).toString("base64");
     // NOTE: Must be url safe base64 encoding, but keep base64 "=" suffix
     // Convert '+' to '-'
     // Convert '/' to '_'
diff --git a/node_modules/@polymarket/clob-client-v2/dist/utilities.js b/node_modules/@polymarket/clob-client-v2/dist/utilities.js
index 84a84e3..3cb0c58 100644
--- a/node_modules/@polymarket/clob-client-v2/dist/utilities.js
+++ b/node_modules/@polymarket/clob-client-v2/dist/utilities.js
@@ -1,4 +1,4 @@
-import { createHash } from "node:crypto";
+import { sha1 } from "@noble/hashes/sha1";
 export const roundNormal = (num, decimals) => {
     if (decimalPlaces(num) <= decimals) {
         return num;
@@ -34,7 +34,7 @@ export const decimalPlaces = (num) => {
  */
 export const generateOrderBookSummaryHash = (orderbook) => {
     orderbook.hash = "";
-    const hash = createHash("sha1").update(JSON.stringify(orderbook)).digest("hex");
+    const hash = Buffer.from(sha1(Buffer.from(JSON.stringify(orderbook), "utf8"))).toString("hex");
     orderbook.hash = hash;
     return hash;
 };
