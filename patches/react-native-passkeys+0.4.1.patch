# PATCH CONTEXT
# Why:
#   - Rainbow's Babel profile compiles the native-module spread to Object.assign,
#     but Hermes does not expose lazy JSI host methods as enumerable properties.
#     Explicit delegates keep get(), cancel(), and isSupported() available.
#   - The iOS module can retain a request after its JS caller times out. Expose
#     cancellation and identify callbacks so a timed-out request releases the
#     native slot without settling a newer request. Android implements the shared
#     bridge method as a no-op because Cash only invokes cancellation on iOS.
# Upstream Issue: https://github.com/peterferguson/react-native-passkeys/issues/13
#   (same missing-method symptom; the issue does not establish the root cause).
# Linear Issue: APP-4117.
# Remove when: react-native-passkeys exposes cancellation and no longer reads its
#   own methods off the spread default export.

diff --git a/node_modules/react-native-passkeys/android/src/main/java/expo/modules/passkeys/ReactNativePasskeysModule.kt b/node_modules/react-native-passkeys/android/src/main/java/expo/modules/passkeys/ReactNativePasskeysModule.kt
index 42f67d0..2601d4b 100644
--- a/node_modules/react-native-passkeys/android/src/main/java/expo/modules/passkeys/ReactNativePasskeysModule.kt
+++ b/node_modules/react-native-passkeys/android/src/main/java/expo/modules/passkeys/ReactNativePasskeysModule.kt
@@ -50,0 +51,2 @@ class ReactNativePasskeysModule : Module() {
+        AsyncFunction("cancel") {}
+
diff --git a/node_modules/react-native-passkeys/build/ReactNativePasskeysModule.js b/node_modules/react-native-passkeys/build/ReactNativePasskeysModule.js
index 4e8d2d1..cb784a4 100644
--- a/node_modules/react-native-passkeys/build/ReactNativePasskeysModule.js
+++ b/node_modules/react-native-passkeys/build/ReactNativePasskeysModule.js
@@ -7,0 +8,6 @@ export default {
+    async get(request) {
+        return await passkeys.get(request);
+    },
+    async cancel() {
+        await passkeys.cancel();
+    },
@@ -9 +15 @@ export default {
-        if (!this.isSupported)
+        if (!passkeys.isSupported())
@@ -23 +29 @@ export default {
-//# sourceMappingURL=ReactNativePasskeysModule.js.map
\ No newline at end of file
+//# sourceMappingURL=ReactNativePasskeysModule.js.map
diff --git a/node_modules/react-native-passkeys/build/index.d.ts b/node_modules/react-native-passkeys/build/index.d.ts
index 4541ef7..944ed03 100644
--- a/node_modules/react-native-passkeys/build/index.d.ts
+++ b/node_modules/react-native-passkeys/build/index.d.ts
@@ -3,0 +4 @@ export declare function isAutoFillAvalilable(): boolean;
+export declare function cancel(): Promise<void>;
@@ -17 +18 @@ export declare function get(request: Omit<PublicKeyCredentialRequestOptionsJSON,
-//# sourceMappingURL=index.d.ts.map
\ No newline at end of file
+//# sourceMappingURL=index.d.ts.map
diff --git a/node_modules/react-native-passkeys/build/index.js b/node_modules/react-native-passkeys/build/index.js
index ee3ef5f..c5c94e8 100644
--- a/node_modules/react-native-passkeys/build/index.js
+++ b/node_modules/react-native-passkeys/build/index.js
@@ -9,0 +10,3 @@ export function isAutoFillAvalilable() {
+export async function cancel() {
+    await ReactNativePasskeysModule.cancel();
+}
@@ -16 +19 @@ export async function get(request) {
-//# sourceMappingURL=index.js.map
\ No newline at end of file
+//# sourceMappingURL=index.js.map
diff --git a/node_modules/react-native-passkeys/ios/PasskeyDelegate.swift b/node_modules/react-native-passkeys/ios/PasskeyDelegate.swift
index c90ec61..6c5ad3f 100644
--- a/node_modules/react-native-passkeys/ios/PasskeyDelegate.swift
+++ b/node_modules/react-native-passkeys/ios/PasskeyDelegate.swift
@@ -6,2 +6,2 @@ protocol PasskeyResultHandler {
-    func onSuccess(_ data: PublicKeyCredentialJSON)
-    func onFailure(_ error: Error)
+    func onSuccess(_ data: PublicKeyCredentialJSON, requestId: UUID)
+    func onFailure(_ error: Error, requestId: UUID)
@@ -13,0 +14 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
+    private let requestId: UUID
@@ -15 +16 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
-    init(handler: PasskeyResultHandler) {
+    init(handler: PasskeyResultHandler, requestId: UUID) {
@@ -16,0 +18 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
+        self.requestId = requestId
@@ -37 +39 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
-        handler.onFailure(error)
+        handler.onFailure(error, requestId: requestId)
@@ -49 +51 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
-                handler.onFailure((ASAuthorizationError(ASAuthorizationError.Code.failed)))
+                handler.onFailure((ASAuthorizationError(ASAuthorizationError.Code.failed)), requestId: requestId)
@@ -97 +99 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
-            handler.onSuccess(Either(registrationResult))
+            handler.onSuccess(Either(registrationResult), requestId: requestId)
@@ -101 +103 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
-                handler.onFailure((ASAuthorizationError(ASAuthorizationError.Code.failed)))
+                handler.onFailure((ASAuthorizationError(ASAuthorizationError.Code.failed)), requestId: requestId)
@@ -120 +122 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
-            handler.onSuccess(Either(registrationResult))
+            handler.onSuccess(Either(registrationResult), requestId: requestId)
@@ -171 +173 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
-            handler.onSuccess(Either(assertionResult))
+            handler.onSuccess(Either(assertionResult), requestId: requestId)
@@ -190 +192 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
-            handler.onSuccess(Either(assertionResult))
+            handler.onSuccess(Either(assertionResult), requestId: requestId)
@@ -192 +194 @@ class PasskeyDelegate: NSObject, ASAuthorizationControllerDelegate,
-            handler.onFailure((ASAuthorizationError(ASAuthorizationError.Code.failed)))
+            handler.onFailure((ASAuthorizationError(ASAuthorizationError.Code.failed)), requestId: requestId)
diff --git a/node_modules/react-native-passkeys/ios/PasskeyExceptions.swift b/node_modules/react-native-passkeys/ios/PasskeyExceptions.swift
index 0138931..215b4b5 100644
--- a/node_modules/react-native-passkeys/ios/PasskeyExceptions.swift
+++ b/node_modules/react-native-passkeys/ios/PasskeyExceptions.swift
@@ -14,0 +15,6 @@ internal class PendingPasskeyRequestException: Exception {
+internal class PasskeyRequestCancelledException: Exception {
+  override var reason: String {
+    "The pending passkey request was cancelled"
+  }
+}
+
diff --git a/node_modules/react-native-passkeys/ios/ReactNativePasskeysModule.swift b/node_modules/react-native-passkeys/ios/ReactNativePasskeysModule.swift
index 06e9aa9..660c1f0 100644
--- a/node_modules/react-native-passkeys/ios/ReactNativePasskeysModule.swift
+++ b/node_modules/react-native-passkeys/ios/ReactNativePasskeysModule.swift
@@ -5,0 +6 @@ struct PasskeyContext {
+    let id: UUID
@@ -7,0 +9 @@ struct PasskeyContext {
+    let authController: ASAuthorizationController
@@ -37,2 +39,2 @@ final public class ReactNativePasskeysModule: Module, PasskeyResultHandler {
-            let passkeyDelegate = PasskeyDelegate(handler: self)
-            passkeyContext = PasskeyContext(passkeyDelegate: passkeyDelegate, promise: promise)
+            let requestId = UUID()
+            let passkeyDelegate = PasskeyDelegate(handler: self, requestId: requestId)
@@ -52,0 +55,5 @@ final public class ReactNativePasskeysModule: Module, PasskeyResultHandler {
+            passkeyContext = PasskeyContext(
+                id: requestId,
+                passkeyDelegate: passkeyDelegate,
+                promise: promise,
+                authController: authController)
@@ -66,2 +73,2 @@ final public class ReactNativePasskeysModule: Module, PasskeyResultHandler {
-            let passkeyDelegate = PasskeyDelegate(handler: self)
-            let context = PasskeyContext(passkeyDelegate: passkeyDelegate, promise: promise)
+            let requestId = UUID()
+            let passkeyDelegate = PasskeyDelegate(handler: self, requestId: requestId)
@@ -107,0 +115,6 @@ final public class ReactNativePasskeysModule: Module, PasskeyResultHandler {
+            let context = PasskeyContext(
+                id: requestId,
+                passkeyDelegate: passkeyDelegate,
+                promise: promise,
+                authController: authController)
+
@@ -112,0 +126,14 @@ final public class ReactNativePasskeysModule: Module, PasskeyResultHandler {
+        AsyncFunction("cancel") {
+            guard let context = self.passkeyContext else {
+                return
+            }
+
+            self.passkeyContext = nil
+            context.authController.delegate = nil
+            context.authController.presentationContextProvider = nil
+            if #available(iOS 16.0, *) {
+                context.authController.cancel()
+            }
+            context.promise.reject(PasskeyRequestCancelledException())
+        }.runOnQueue(.main)
+
@@ -131,3 +158,3 @@ final public class ReactNativePasskeysModule: Module, PasskeyResultHandler {
-    internal func onSuccess(_ data: PublicKeyCredentialJSON) {
-        guard let promise = passkeyContext?.promise else {
-            log.error("Passkey context has been lost")
+    internal func onSuccess(_ data: PublicKeyCredentialJSON, requestId: UUID) {
+        guard let context = passkeyContext, context.id == requestId else {
+            log.error("Passkey context has been lost or superseded")
@@ -136,0 +164 @@ final public class ReactNativePasskeysModule: Module, PasskeyResultHandler {
+        let promise = context.promise
@@ -149,3 +177,3 @@ final public class ReactNativePasskeysModule: Module, PasskeyResultHandler {
-    internal func onFailure(_ error: Error) {
-        guard let promise = passkeyContext?.promise else {
-            log.error("Passkey context has been lost")
+    internal func onFailure(_ error: Error, requestId: UUID) {
+        guard let context = passkeyContext, context.id == requestId else {
+            log.error("Passkey context has been lost or superseded")
@@ -154,0 +183 @@ final public class ReactNativePasskeysModule: Module, PasskeyResultHandler {
+        let promise = context.promise
diff --git a/node_modules/react-native-passkeys/src/ReactNativePasskeysModule.ts b/node_modules/react-native-passkeys/src/ReactNativePasskeysModule.ts
index 22b8020..6d744de 100644
--- a/node_modules/react-native-passkeys/src/ReactNativePasskeysModule.ts
+++ b/node_modules/react-native-passkeys/src/ReactNativePasskeysModule.ts
@@ -16,0 +17,4 @@ export default {
+	async cancel(): Promise<void> {
+		await passkeys.cancel();
+	},
+
diff --git a/node_modules/react-native-passkeys/src/index.ts b/node_modules/react-native-passkeys/src/index.ts
index 8713ab6..5395535 100644
--- a/node_modules/react-native-passkeys/src/index.ts
+++ b/node_modules/react-native-passkeys/src/index.ts
@@ -21,0 +22,4 @@ export function isAutoFillAvalilable(): boolean {
+export async function cancel(): Promise<void> {
+	await ReactNativePasskeysModule.cancel();
+}
+
