appId: ${APP_ID}
---
- openLink: rainbow://e2e/setExperimentalFlag?flag=${FLAG}&value=${VALUE}
- runFlow:
    when:
      visible: 'Open in .*'
      platform: iOS
    commands:
      - tapOn: 'Open'
