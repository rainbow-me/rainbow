appId: ${APP_ID}
env:
  PKEY: ${DEV_PKEY}
  NAME: 'Test Wallet'
---
- assertVisible:
    id: welcome-screen
- openLink: rainbow://e2e/import?privateKey=${PKEY}&name=${NAME}
- assertVisible:
    id: wallet-screen
