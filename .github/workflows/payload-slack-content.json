{
    "blocks": [{
        "type": "section",
        "text": {
            "type": "mrkdwn",
            "text": "Build triggered by ${{ github.actor }}\n#${{ github.event.pull_request.number }} - ${{ }}\nStatus: ${{ job.status }}"
          }
    }]
}
