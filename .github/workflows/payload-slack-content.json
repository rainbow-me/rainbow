{
    "username": "iOS Build Bot",
    "blocks": [{
        "type": "section",
        "text": {
            "type": "mrkdwn",
            "text": "iOS Build triggered by ${{ github.actor }}\n#${{ github.event.pull_request.number }} - ${{ }}\nStatus: ${{ job.status }}"
          }
    }]
}
