/**
 * @desc Deposit an asset in Compund.finance
 * @param  {String} asset,
 * @param  {Object} amount,
 * @param  {Object} gas,
 * @return {Promise}
 */
export default function depositIntoCompund(asset, amount, gas) {
  console.log(asset, amount, gas);
}
