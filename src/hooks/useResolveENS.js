import { web3Provider } from '../../handlers/web3';
import { abbreviations } from '../utils';

/**
 * Pass a users ETH wallet address
 * Resolve the ens name OR truncate the eth address and return it
 */
export default function useResolveENS({ address, truncation }) {
  
}
